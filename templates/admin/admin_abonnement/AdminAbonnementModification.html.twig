{% extends 'base.html.twig' %}

{% block monTitre %}
  {% if isModification %}
    Modification {{abo.name}}
  {% else %}
    Ajout d'un abonnement
  {% endif %}
{% endblock %}

{% block body %}

{{form_start(form)}}
  <div class="form_produit">
    <div>{{form_row(form.name, {'attr' : {'class' : 'text-danger'}, 'label' : 'Nom de l\'abonnement'})}}</div>
    <div>{{form_row(form.description)}}</div>
    <div>{{form_row(form.price)}}</div>
    <div>{{form_row(form.options)}}</div>
    <div>{{form_row(form.imageFile)}}</div>
    {% if isModification %}
      <input type="submit" class="btn btn-primary" value="Sauvegarder les modifications" style="width:100%">
    {% else %}
      <input type="submit" class="btn btn-primary" value="Créer" style="width:100%">
    {% endif %}
  </div>
{{form_end(form)}}

{% endblock %}
