{% extends 'base.html.twig' %}

{% block body %}
  <div class="jumbotron">
    <h1 class="display-3 text-center">{{ abo.name }}</h1>
    <p class="lead"></p>
    <hr class="my-4">
    <p>{{ abo.description }} : {{ lipsum }}</p>
    <div class="d-inline px-5">
      <div class="row">
        <p class="col-2">{{lipsum}}</p>
        <img class="col rounded mx-auto d-block" alt="Responsive image" src="{{asset('pictures/abonnements/' ~ abo.image) }}">
        <p class="col-2 text-center font-weight-bolder">{{abo.price}}€</p>
      </div>
      <p class="lead">
        <a class="btn btn-primary btn-lg btn-block m-3" href="#" role="button">Choisir cet abonnement</a>
      </p>
    </div>
  </div>
{% endblock %}
